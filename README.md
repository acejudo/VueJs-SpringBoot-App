# Run docker-compose
docker-compose up

# Run docker compose in background
docker-compose up -d 

# Stop docker compose
docker-compose stop

# Force recreate build
docker-compose up --force-recreate --build -d